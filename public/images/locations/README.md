# Local location thumbnails

Place self-created or properly licensed 16:9 thumbnails in this directory.
Reference them from a street view entry with a path relative to `public/`:

```json
"local_image_path": "images/locations/argentina/example.webp"
```

An empty string keeps the local preview unavailable. Do not commit copies of
Google Street View imagery unless the applicable Google terms expressly allow it.
